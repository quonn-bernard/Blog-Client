import React from 'react';
import "./Footer.css"

export default function Footer() {
    // let endpoints = ["register","login",""]
    // if(window.location.href !== )
    // console.log(window.location.href)
    return (
        <div className="Footer fixedBottom">
         {/* <div className={(window.location.href !== `http://localhost:3000/blogFeed`) ? "Footer fixedBottom" : "Footer"}> */}
            <small>© Copyright 2019. Designed by Quonn Bernard</small>
        </div>
    )
}